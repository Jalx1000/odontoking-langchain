# Sprint 1 — qa-dev Tasks

**Agent:** qa-dev  
**Sprint:** 1 — Platform Router  
**Status:** pending  
**Can start:** after P-2 is implemented (internal endpoint exists to test)  

---

## Task Q-1 — Tests for POST /internal/usage

**Priority:** high  
**Depends on:** P-2 (endpoint implemented)  

### What to do

**Create:** `tests/api/test_internal.py`

Write 5 tests using pytest + FastAPI TestClient:

1. **valid key + valid tenant → 200** — creates UsageLog row for today
2. **valid key + same tenant second call → 200** — increments existing row (not duplicate)
3. **invalid key → 401** — returns `{"detail": "Invalid internal API key."}`
4. **missing key header → 401** — same as invalid
5. **valid key + unknown slug → 404** — returns `{"detail": "Tenant not found."}`

Use a fixture that creates a test tenant in the DB.
Do NOT mock the DB — use the test database.

### Deliverable

- [ ] 5 tests written and passing
- [ ] Tests cover upsert logic (idempotent increments)
- [ ] `uv run pytest tests/api/test_internal.py -v` → all green

---

## Task Q-2 — Regression test for _AGENT_REGISTRY change (P-1)

**Priority:** medium  
**Depends on:** P-1  

### What to do

Add a test to `tests/unit/test_whatsapp.py` (create if doesn't exist):

1. **Registry keys on agent_type:** Create a mock TenantConfig with `slug="odontoking-dev"` and `agent_type="odontoking"`. Verify `_make_process_fn` resolves to `odontoking_agent` (not None, not fallback from slug).

2. **Unknown agent_type falls back:** TenantConfig with `agent_type="unknown-future-agent"` resolves to `odontoking_agent` (the defined fallback).

### Deliverable

- [ ] 2 regression tests passing
- [ ] Tests imported from `app.api.v1.whatsapp import _AGENT_REGISTRY, _make_process_fn`

---

## Risk flags from lead-dev

- Q-1 test for "second call" is critical — the upsert logic (not insert-duplicate) must be explicitly tested
- Do not mock `settings.INTERNAL_API_KEY` — set it via env or monkeypatch for the test
